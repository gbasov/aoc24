{
    "name": "aoc24",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "create:day": "new_dir=$(printf '%02d' $(($(ls -d [0-9][0-9] 2>/dev/null | sort -n | tail -n 1 | sed 's/^0*//') + 1))); mkdir $new_dir && cp -r tmpl/* $new_dir && echo \"Created day $new_dir\""
    },
    "author": "",
    "license": "ISC",
    "description": "",
    "devDependencies": {
        "@types/node": "^22.10.1",
        "prettier": "^3.4.2"
    },
    "prettier": {
        "tabWidth": 4,
        "semi": false,
        "singleQuote": true
    }
}
